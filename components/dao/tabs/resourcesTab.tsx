import { Heading, VStack } from "@chakra-ui/layout";

const ResourcesTabPanel = () => {
  return (
    <VStack>
      <Heading>Resources for the DAO</Heading>
    </VStack>
  );
};

export default ResourcesTabPanel;
